{
    "pluginAlias": "ThermostatAccessory",
    "pluginType": "accessory",
    "schema": {
        "name": {
            "type": "string",
            "description": "The name of your thermostat accessory.",
            "required": true,
            "default": "Thermostat",
            "placeholder": "Thermostat"
        },
        "apiGetTemperature": {
            "type": "string",
            "description": "The URL to fetch the current temperature.",
            "required": true,
            "placeholder": "http://yourURL.com/getTemperature"
        },
        "apiSetTemperature": {
            "type": "string",
            "description": "The URL to set the target temperature.",
            "required": true,
            "placeholder": "http://yourURL.com/setTargetTemperature"
        },
        "apiSetOFF": {
            "type": "string",
            "description": "The URL to set the state to OFF.",
            "required": true,
            "placeholder": "http://yourURL.com/heatCoolingStateOFF"
        },
        "apiGetToken" : {
            "type": "string",
            "description": "The token used for API authentication (if required). This is for the getTemperature",
            "required": false,
            "placeholder": "Your Bearer Token"
        },
        "apiContentType" : {
            "type": "string",
            "description": "The contentType used. If not populated, default value will be used",
            "required": false,
            "placeholder": "application/json"
        }
    }
}
